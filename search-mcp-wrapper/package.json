{
  "name": "search-mcp-wrapper",
  "version": "0.1.0",
  "description": "MCP 反-bot 节流 wrapper（方案 C），包裹 duckduckgo-websearch：强制 max_results<=10 + 跨调用状态 + 指数退避熔断",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "test": "tsc && node --test build/test/integration.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "duckduckgo-websearch": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "license": "MIT"
}
